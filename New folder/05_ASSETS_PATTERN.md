# Assets Pattern

Pattern name: Asset Registry

Rules:
- Images are stored in object storage
- Database stores metadata only
- Assets have a type: person | dress | result
- Assets belong to exactly one user
- Assets may be reused if marked as library items
